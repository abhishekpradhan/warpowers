; War Powers original data — 2D animations (drawable status icons).
; Icon names are engine-fixed (TheDrawableIconNames); missing templates used
; to null-crash Drawable icon draws (engine now guards, but the icons are
; product surface anyway).
Animation DefaultHeal
  NumberImages = 1
  Image = WPHealIcon
  AnimationMode = LOOP
  AnimationDelay = 100
End

Animation StructureHeal
  NumberImages = 1
  Image = WPHealIcon
  AnimationMode = LOOP
  AnimationDelay = 100
End

Animation VehicleHeal
  NumberImages = 1
  Image = WPHealIcon
  AnimationMode = LOOP
  AnimationDelay = 100
End

Animation Disabled
  NumberImages = 1
  Image = WPNoPowerIcon
  AnimationMode = LOOP
  AnimationDelay = 100
End
