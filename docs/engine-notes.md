# Engine Notes — GeneralsX/ZH boot requirements (for the zero-retail dataset)

Condensed from a full source exploration + empirical bring-up, 2026-08-21. The Zero Hour build is the `GeneralsMD/` tree (`z_generals` target → `GeneralsXZH`); shared code in `Core/`; `GeneralsZH/` holds only GeneralsX's replacement `ExtrasMenu.wnd`.

## The three-bucket shape of the minimal dataset

1. **~46 must-exist INI paths** — the engine hard-dies (`throw INI_CANT_OPEN_FILE` → `RELEASE_CRASH`) on any missing one, but shrugs at dangling references inside them (all cross-reference validators are no-op in release). Each `Data\INI\X` demand is satisfied by `Data/INI/X.ini` *or* any `.ini` inside `Data/INI/X/`. Full list lives in our authored dataset (`data/Data/INI/**`).
2. **Exactly one must-exist binary asset:** ≥1 music file — `GameEngine.cpp:622` silently sets quitting if no `MusicTrack`'s resolved file exists on disk. Path = `AudioSettings.AudioRoot\MusicFolder\Filename`. (Ours: the 6-track music rotation; originally a stub `wp_ambient_01.mp3`.)
3. **A handful of must-exist WND layouts** whose absence causes null-deref segfaults, not clean errors: `Menus/MainMenu.wnd`, `Menus/BlankWindow.wnd`, `ControlBar.wnd` (+ every named child the code references — we generate all 117), `GeneralsExpPoints.wnd`, `ControlBarPopupDescription.wnd`, `ReplayControl.wnd`. Missing WND *file* → `winCreateFromScript` returns null harmlessly; missing named *window* → crash at the unguarded lookups.

Everything visual degrades gracefully: missing W3D model → invisible object; missing texture → magenta placeholder; missing shader → logged error; missing CSF strings → raw labels.

## Key mechanics

- **Loose files beat archives; archives are optional.** `StdBIGFileSystem` mounts every `*.big` (no hardcoded names) but boots happily with zero. Ship everything loose.
- **Asset root resolution** (in order): `$CNC_GENERALS_ZH_PATH` → `$GENERALSX_ASSET_PATH` → `$CNC_ZH_INSTALLPATH` → `Options.ini [Paths]` → registry.ini → `<exedir>/../Resources` → `<exedir>` → CWD. run.sh cd's to the game dir, so CWD wins.
- **User data dir** (saves, maps, registry.ini): `~/Library/Application Support/GeneralsX/GeneralsZH/`. User maps ARE disk-scanned; asset-root `Maps/` needs `MapCache.ini` unless `-buildmapcache` (which we patched into release).
- **Singleton INI blocks** must exist or `OVERRIDE<T>` globals stay null and later deref: `WaterSet <TOD>` ×4 + `WaterTransparency` (Water.ini), `Weather` (Weather.ini). Same pattern likely for others as we go deeper.
- **`Object DefaultThingTemplate` is load-bearing** (ThingFactory.cpp:137): every new object template copies its baseline from a template literally named `DefaultThingTemplate`. The ThingTemplate ctor never initializes `m_assetScale`, so without this template every model is created at **scale 0** (vertices collapse to −0.0 — invisible, no error). Ours sets `Scale = 1.0`; retail's sets many more baselines — expect more zero-default finds seeded here.
- **`W3DDefaultDraw` is a no-op in release builds** (constructor body is `#ifdef LOAD_TEST_ASSETS`); INI-declared objects must use `W3DModelDraw` with `DefaultConditionState / Model = <name> / End`.
- **GameData zero-defaults found so far** that retail INI normally sets: `PartitionCellSize` (0 → shroud div-by-zero), `CameraPitch/CameraHeight` (0 → horizon-staring camera). `ShroudOn = No` is a valid data toggle.
- **W3D models**: `tools/genw3d.py` generates engine-valid meshes (byte spec from the GPL reader; see the tool). **RESOLVED 2026-08-22 — the "invisible mesh bug" was never a rendering bug.** Meshes rendered correctly all along; in every unattended run the mouse cursor sat at screen corner (0,0), the RTS **edge-scroll** marched the camera off the base within ~2s of match start, and the meshes were then *correctly* frustum-culled. They did render during the first frames — under the fade-from-black — which is exactly the window all budget-capped instrumentation sampled, manufacturing days of false "draws submit, no pixels" evidence. Fix for the headless dev loop: `HorizontalScrollSpeedFactor = 0.0` / `VerticalScrollSpeedFactor = 0.0` in GameData (restore for real input work). Two secondary data findings: `UseBehindBuildingMarker = No` (with it on, structures are routed to the stencil-occlusion flush path, unverified on this port), and maps must be big enough for the camera-area constraints at CameraHeight (a 640×640-world map collapses the constraint box; WPTest is now 160×160 cells = 1600×1600). **Lessons burned in:** (1) never trust budget-capped logging to characterize steady-state behavior — always run an unbudgeted long-horizon counter first; (2) before instrumenting a draw pipeline, verify the camera is looking at the object *in the failing frames*, not in frame 1. The GPU-forensics detour still paid: three real latent bugs found and fixed in the DXVK fork (see below), plus a validated Metal/Vulkan instrumentation toolkit (`WP_DXVK_SPY` in the fork; MoltenVK-source spies in `~/MoltenVK-src`, not shipped).
- **Transition groups:** every name in code must exist in `WindowTransitions.ini` (56 names, predefined) — engine bug: `remove()` on unknown name null-derefs via `null == null` (patched in our branch).
- **The `-file` direct-load path fights the shell, twice** (both fixed on our branch, 2026-08-22). (1) Nothing pops the shell: MainMenu's full-screen window stays visible-status over the running game and swallows every drawable pick in `W3DView::pickDrawable` (`getWindowUnderCursor`), so clicks select nothing — fixed by popping shell screens in `GameLogic::startNewGame`. (2) With the menus popped, `Intro::doPostIntro`'s **render freeze** (`m_breakTheMovie=TRUE`, meant to hold the screen until MainMenu fades in and normally cleared by `MainMenuInit`) is never cleared — `W3DDisplay::draw` then skips its entire scene-render block forever. The game still *runs* (logic, input, castRay all live) but no `Visibility_Check` ever executes, so every render object keeps its creation-default visibility bit: map-placed objects happen to be visible, production-spawned units stay `Is_Really_Visible()=0` — invisible AND unpickable (castRay gates on it). Cost days of shroud/ghost-object false trails; the tell that cracked it was instrumenting `Visibility_Check` and finding it never ran. Fix pair: clear the flag in `startNewGame`, and gate `doPostIntro` on `TheShell->top()` existing. Lesson: when a *state bit* is wrong, first verify the *writer of that bit runs at all* before theorizing about which write is wrong.
- **Fog of war works with 100% original data**: `ShroudOn = Yes` (retail default) runs the full partition shroud pipeline — enemy objects beyond vision are `OBJECTSHROUD_SHROUDED` and unrenderable/unpickable, own-vision circles clear via `VisionRange`/`ShroudClearingRange`, terrain shroud draws as black falloff. The ghost-object snapshot system (fogged-building memory) also engages cleanly.
- **Win/lose conditions in `-file` mode are map scripts** (VictoryConditions proper is multiplayer-gated). The `PlayerScriptsList` chunk must be **nested inside `SidesList`** (the map reader only registers it there — `SidesList::ParseSidesDataChunk`; the top-level registration is for `.scb` files only); `ScriptList` sub-chunk order maps 1:1 to side order. Chunk formats from the Scripts.cpp writers: Script v2 (name + 3 comment strings + 6 flag bytes + delayEvaluationSeconds int) containing OrCondition v1 (nested Condition v4 chunks) + ScriptAction v2 chunks. **v4/v2 condition/action chunks carry the internal-name key and the engine rematches the type by name** — write ordinal 0 plus the name (`NAMED_DESTROYED`, `VICTORY`, `LOCALDEFEAT`) and skip fragile enum counting; Parameter type ordinals ARE positional (`Parameter::UNIT` = 14; params serialize as int type + int + float + string, COORD3D excepted). Map objects get script names via the `objectName` dict key (→ `Object::setName` → the script engine's named-object cache; `NAMED_DESTROYED` also stays true after the object is fully removed, via `didUnitExist`). `VICTORY` = input off + 120-frame banner + `exitGame`; after a `-file` match the engine already quits to desktop (`m_initialFile` check in `GameLogic::clearGameData`). Two crash fixes on the way out (our branch): `Shell::doPush` null-derefs when the pushed layout file is missing (post-game `ScoreScreen.wnd`); `~DX8VertexBufferClass`/`~DX8IndexBufferClass` deref dead D3D buffers when subsystems (TerrainTracks via `~GameClient`) are destroyed after device shutdown — this was the "pre-existing SIGTERM shutdown crash", and it fires on every clean quit too.
- **Flags in release build:** `-win -fullscreen -headless -xres -yres -noshellmap -noShellAnim -nologo -quickstart -mod -noshaders`, plus our patch adds `-file <map>` (menu bypass) and `-buildmapcache`. `-map/-noaudio/-nomusic/-novideo` remain debug-only upstream. **`-headless` skips ALL GUI/WND/SDL/Vulkan — the future CI harness.**
- **Language:** resolves to `english` via the port's registry.ini default; `Data/english/` holds Language, HeaderTemplate, CommandMap.

## macOS build gotchas (hard-won)

- Build needs `VCPKG_ROOT` and the LunarG SDK env: `source ~/VulkanSDK/1.4.357.1/setup-env.sh` (the build script checks the SDK but doesn't export `VULKAN_SDK` — upstreamable fix).
- `RTS_DEBUG_CRASHING=ON` does not compile on macOS (MSVC `__int64` in Debug.h profile structs).
- **Never `cp` over a previously-executed binary** — same-inode replacement trips the kernel's signature cache → instant SIGKILL (exit 137, zero output). `rm` first (deploy script needs this fix).
- Piping build commands through `tail` masks their exit codes. Don't.
- Crash-report line attribution in RelWithDebInfo is unreliable (a "ThingFactory::reset" stack was actually a different site); `fprintf` breadcrumbs + rebuild beat forensics.
- **DYLD_INSERT_LIBRARIES is stripped when exec'ing SIP-protected binaries** (i.e. `/bin/bash`, so any env you set before `run.sh` is gone by the time the game launches). Compose the full env inline and exec `./GeneralsXZH` directly, the way our test harness does.
- **Never touch Metal (create a device/encoder) inside a dyld initializer** — the AGX driver's lazy internal-shader compile aborts the process (SIGABRT in `getRangeExecutionVariant`). Defer to a dispatch_after.
- **Present blocks forever when the window is occluded** (locked screen): CAMetalLayer starves drawables → DXVK `Presenter::acquireNextImage` waits on a condvar indefinitely. Use `WP_PRESENT_SKIP=100000000` (engine feature) for headless runs; `WP_FRAME_DUMP` still captures real frames. Also: a game stuck there survives `pkill` (TERM) — a zombie instance then poisons later runs; use `pkill -9` and check `pgrep` before every run.
- The engine's own shutdown crashes in `TerrainTracksRenderObjClassSystem::shutdown()` on SIGTERM (pre-existing destructor bug, harmless noise in crash reports).
- ObjC-swizzle spies cannot see MoltenVK's encoder calls usefully (dispatch goes through per-frame recreated encoders + selector stubs; our hooks verified installed yet never fired for reasons ultimately mooted by the real root cause). The productive instrumentation layers are: DXVK source (we own it), MoltenVK source (built from `~/MoltenVK-src`, `make macos` with `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer`), and engine breadcrumbs.

## DXVK fork fixes found during the hunt (all committed on `warpowers-dxvk`)

1. **Dummy vertex binding bound with length 0** → every null-attribute fetch was out-of-bounds, undefined without robustness2 (the exact configuration the fallback exists for). Now binds the dummy's full length.
2. **Dummy vertex binding stride 0** → with dynamic strides, Metal gets `attributeStride:0` on a per-vertex layout and the AGX driver silently kills the draw. Visible symptom: far-terrain rows missing (their fix restored them). Now stride 16.
3. **Spec-constant UBO never created without `VK_EXT_graphics_pipeline_library`** (which MoltenVK lacks; our patchset waives DXVK's requirement) → every shader's `spec_state` pointer in the Metal argument buffer was NULL, a latent GPU null-deref for any pipeline variant that reads it. Now always created/bound on `__APPLE__`.

- **The build-button UI works with generated windows** (2026-08-22 late). The old "ControlBar window-lifecycle mystery" dissolved on measurement: the ControlBar layout is created once in `InGameUI::init` → `createControlBar()` and never destroyed — the failing `MoneyDisplay`/`PowerWindow` lookups failed because our hand-authored ControlBar.wnd simply never contained those windows, and the old money/power debug bail then silently returned out of `InGameUI::update` *before* `TheControlBar->update()`, so the whole context-sensitive command UI never ran in any match. `tools/genwnd.py` now generates ControlBar.wnd (48 windows): `ButtonCommand01..18` (`MAX_COMMANDS_PER_SET`) under `CommandWindow`, `ButtonQueue01..09` (`MAX_BUILD_QUEUE_BUTTONS` — **deref'd unguarded** in `populateBuildQueue`, hard requirement) under `ProductionQueueWindow`, `MoneyDisplay` (STATICTEXT) + `PowerWindow`. Click wiring: a gadget's GBM_SELECTED goes to its *owner* (itself by default), so buttons and their containers need `SYSTEMCALLBACK = "PassSelectedButtonsToParentSystem"` to bubble the message up to `ControlBarParent`, whose `"ControlBarSystem"` callback routes unrecognized button ids to `ControlBar::processContextSensitiveButtonClick` → the CommandButton's game message (UNIT_BUILD → `MSG_QUEUE_UNIT_CREATE`). Data side was already correct (CommandSet/CommandButton INI + `CommandSet =` on the object; `CANMAKE_OK == 0` — a logged `canMake=0` is success, not failure). Verified self-driving (`WP_CLICKTEST=ui`): click CC → selectCount=1 → ButtonCommand01 visible+enabled → click it → prodQ=1 → tank spawns. Also: `WP_FRAME_DUMP` TGAs are **top-down** (descriptor bit 0x20) — decode accordingly, or text looks mirrored and the UI looks upside-down (cost an hour chasing a nonexistent y-flip bug; `tgadiff.py` was always right).

- **Hidden tabs suspend the game entirely** (browser suspends `requestAnimationFrame` → the emscripten main loop never ticks → logic frozen, fade-from-black stuck, HUD fps garbage). Correct product behavior (auto-pause), but it perfectly impersonates an engine freeze during agent/pane-driven testing — check pane visibility before diagnosing "frozen logic" in the browser. In-browser interactive verification requires the pane visible; logic correctness stays covered by the native `WP_CLICKTEST`/`WP_AUTOTEST` harness.
- **Product shell (2026-08-22 night):** `web/index.html` is now a designed page — wordmark loading screen with byte-accurate progress (manifest carries file sizes; wasm fetched via `Module.instantiateWasm` with streaming byte counts), phase captions, error states with reload, letterboxed responsive canvas, hover fullscreen control, inline SVG favicon. Debug chrome is opt-in via `?debug=1` (log panel, `IG_TRACE=1`, in-game HUD via Options.ini `RenderFpsFontSize/SystemTimeFontSize/GameTimeFontSize` — 0 hides, engine default is 8/on). Console spam fixed: missing-label logged once (GameText), LSFS + d8web atlas probes behind IG_TRACE (d8web patched in the local dvijoke checkout — fork dvijoke at publish for provenance, add to D012 list). *[Superseded 2026-08-31: dvijoke is vendored inline in the workspace instead and its fork repo archived — D020.]*

## Stage 2 web build — FIRST BROWSER MATCH (2026-08-22 night)

**The Boot Slice match runs in a browser tab: 12MB of loose original data, no .big archives, full loop verified — CC select → build button → tank ($600 deducts) → move order pathfinds.** Engine branch `warpowers-web` = `warpowers` + merged `meerzulee/GeneralsXWeb` branch `igroteka-wasm` (GPL, same GeneralsX lineage; shared ancestor was already in our history so the ~3000-line patchset merged with six small conflicts). Renderer is **d8web** (github.com/meerzulee/dvijoke, MIT): D3D8→WebGL2, statically linked, expects a sibling checkout at `engine/../dvijoke`. Build: `emcmake cmake --preset wasm && cmake --build build/wasm --target GeneralsXZH.js` (emsdk latest; plain `cmake --preset wasm` silently uses the HOST compiler — always `emcmake`, and a stale host-configured build dir must be deleted). Post-merge fixes our newer base needed: emscripten window path in SDL3Main (no `SDL_WINDOW_VULKAN`), `__EMSCRIPTEN__` exclusion on the glibc `__forced_unwind` catch guards upstream added to the GameSpy threads, `RTS_BUILD_OPTION_DEEP_CRC=OFF` in the wasm preset. Web-side truths: loose-file asset root = process cwd → `FS.chdir('/game')` in the boot page (the `[ASSET_ROOT]` resolver only accepts `.big`-bearing dirs and its fallthrough leaves cwd at `/`); **MEMFS is case-sensitive where macOS wasn't** — first casualty was lowercase `data/Generals.str`, fixed with `FS.symlink('/game/Data','/game/data')`; expect more case bugs as data grows. Serving: `tools/genwebstage.py` → `webstage/` (engine js+wasm, gamedata + manifest, Liberation Sans for the wasm fontconfig stub) + any static server. Pending: native re-verification of the merged branch (their INI/mouse/SubsystemInterface changes also touch native paths), performance measurement, audio (their README lists it missing; MiniAudio partial), win-condition browser run, workspace submodule strategy once `warpowers-web` stabilizes.

## Running log (2026-08-22 → present, newest entries at the bottom)

**Debug scaffolding stripped (2026-08-22 late):** the temporary bring-up traces are gone from the engine branch; what remains, env-gated, is the permanent toolkit — `WP_FRAME_DUMP` (backbuffer TGA every 60th frame, top-down origin), `WP_PRESENT_SKIP` (headless present bypass), `WP_AUTOTEST` (scripted logic smoke test; `=build` stops after tank spawn), `WP_CLICKTEST` (synthetic-SDL input test; `=ui` drives the ControlBar build-button path). Those four are the regression harness — run both CLICKTEST modes after engine changes. `references/fbraz3-dxvk` is now a real engine submodule. Repo publish plan (org + true GitHub forks) is decisions.md D012.

**THE BOOT SLICE IS COMPLETE — a full match runs start to finish on 100% original data.** Mouse input verified self-driving (`WP_CLICKTEST=1`: click tank → picked=1/selectCount=1 → ground click → pathfound move) AND live by the user (real mouse: select, move, camera scroll). Win condition verified end-to-end: map scripts loaded from genmap's nested PlayerScriptsList → tank drives cross-map → destroys enemy CC → `NAMED_DESTROYED(EnemyCC)` → `VICTORY` → input disabled → engine quits to desktop cleanly (`-file` one-shot match behavior). Frame dump also confirms fog-of-war + the tank rendering at its post-move position. `WP_Lose` (`NAMED_DESTROYED(PlayerCC)` → `LOCALDEFEAT`) shares the verified parse path but its runtime firing is untested (inert enemy can't attack). Balance for human play: WP_TankGun 100 dmg (solo CC kill ≈40s), tank recolored olive vs the white CC. Remaining polish before Stage 2: strip `@debug` traces; `InGameUI::update` money/power HUD bail (window lifecycle across end-of-init reset still to be understood); LocalDefeat path test once an enemy can fight (creative round gives it units).
- **`TerrainLOD = AUTOMATIC` (default) breaks shadows and particles on this port** (2026-08-22, beauty target). The first `W3DDisplay::draw` runs `calculateTerrainLOD()` — a 20-sample render benchmark that sets `drawTerrainOnly(true)` and on this port exits without restoring it, so `RTS3DScene::Customized_Render` early-returns before its tail forever: `queueShadows`/`queueParticleRender` never arm (`isShadowScene=0` every frame) while objects still draw through another path. Our GameData pins `TerrainLOD = MAX`. Decal shadows then just work: `UseShadowDecals = Yes` (GameData), `Shadow = SHADOW_DECAL` + `ShadowSizeX/Y` per template, default texture `shadow.tga` (multiplicative: white=no-op — make the visible fringe LARGER than the model footprint or it hides underneath; that cost an hour). Sizes ~2× footprint read right; small `ShadowOffsetX/Y` sells the sun angle.
- **VFX pipeline works end-to-end with authored data** (2026-08-22): ParticleSystem.ini (keyframe syntax `Color1 = R:x G:y B:z frame`, `Alpha1 = lo hi frame`; enum lists in ParticleSys.h), FXList.ini nuggets (`ParticleSystem/ViewShake/TerrainScorch...`), Weapon `FireFX`. Templates load at `ParticleSystemManager::init` (before FXListStore, before WeaponStore — reference order is safe). Gotchas: `m_maxParticleCount` GlobalData default is **0** (LOD apply normally rescues it to the preset's 2500; `ALWAYS_RENDER` priority is exempt from the cap); **`ProjectileDetonationFX` never fires on projectile-less weapons** (impact FX must ride `FireFX` until the weapon gets a real `ProjectileObject` — queued for the roster pass); and verification-wise, a 6-frame flash is invisible to 150-frame status sampling — the autotest now prints live particle counts, and per-frame print windows are the diagnosis tool.
- **Blender→W3D pipeline VALIDATED (2026-08-22, D015 critical path).** Blender 5.2 LTS + OpenSAGE.BlenderPlugin 0.7.4 (needs its `blender_addon_updater` submodule inited; register headless via `sys.path` + `io_mesh_w3d.register()`). Export: `bpy.ops.export_mesh.westwood_w3d(filepath, file_format='W3D', export_mode='HM', force_vertex_materials=True)` — container/HLod name comes from the FILENAME (lowercase is fine; engine name-keys are case-insensitive), mesh name from the Blender mesh datablock, texture stage + UVs from a Principled BSDF image-texture node, image saved separately as TGA into `Art/Textures`. Verified in-engine: a beveled UV-textured test mesh exported headless renders in place of the Vector with working texture and shadow. `tools/blender/spike_export.py` is the reference script. The hero-asset workflow this unlocks: bmesh modeling + Smart UV + AO/procedural bakes to a painted-style TGA + W3D export, fully scripted.
- **First hero asset shipped (2026-08-22): the Vector, via the scripted Blender pipeline** (`tools/blender/build_vector.py`). Recipe that works: bmesh side-profile prisms for hull silhouettes (sloped glacis, stepped decks), primitive+bevel for turrets, cylinders for wheels/barrels; join; Smart UV (256px atlas); Cycles CPU bakes — DIFFUSE color pass + AO — composited in-script (`diffuse * (0.52+0.48*AO^1.4)` + fine grain) to a TGA; re-material with the baked image; plugin export. 792 tris. Reads correctly in-engine at RTS distance. v2 backlog: stronger hull/turret color separation, panel lines in the composite, pointiness edge-wear, team-color region, hull/turret split over an armature for traverse.
- **Pack conversion works through the same pipeline (2026-08-22): Quaternius Tank3 → Jackal Mongrel** (`tools/blender/convert_mongrel.py`). FBX import → strip rig (`vertex_groups.clear()` or the exporter treats the mesh as skinned and fails "not rigged to any bone") → normalize (rotate long axis to X, scale to game units, ground at z=0) → retint → same Smart UV + bake + composite + export as the hero. Two landmines: (1) **imported FBX material datablocks bake BLACK in headless Cycles** — `bpy.ops.object.bake` returns FINISHED but writes nothing; replace them with fresh `bpy.data.materials.new()` datablocks (map old slot → new color) and the identical bake works. (2) Imported meshes carry far more UV islands than hero builds (6544 tris → thousands of islands); a 256px atlas leaves sub-pixel islands unbaked → black seam speckles in-engine. 512px atlas + `island_margin=0.002` + bake `margin=8` cleans it up.
- **Composite v2 + hero buildings (2026-08-22): shared pipeline module** (`tools/blender/wp_pipeline.py`) — all model scripts (heroes AND conversions) now funnel bake/composite/export through one module, so look upgrades land everywhere at once. v2 recipe adds: an EMIT island-mask bake (margin=0) → island-border texels get a noise-broken highlight (smart_project splits at hard edges, so island borders ARE mesh edges → painted edge-wear on every edge for free), plus low-frequency value noise to break flat fields. Team-color masking deferred until the engine's house-color mechanism is mapped. Buildings: `build_mercc.py` (Meridian CC, 740 tris) + `build_jakcp.py` (Jackal CP, 964 tris) replace the part-language placeholders, keeping their identity tokens (terraced white + gold cornice + tower; shack + tarp + watchtower). `Kit` class = palette-bound box/cyl/prism primitives joined into one mesh. `WP_PREVIEW=1 tools/genmap.py` parks enemy structures in view of the start camera for art review without driving across the map.
- **Combat-juice landmines (2026-08-23 overnight).** (1) `AIData.ini` empty stub = **no unit ever auto-acquires**: `GuardInner/OuterModifier*` default to 0 in TAiData, and `AI::getAdjustedVisionRangeForObject` multiplies vision by them → acquire range 0 engine-wide. Same zero-default class as m_maxParticleCount. Our authored AIData block fixes it; with it, map `playerEnemies` dicts (already in genmap) give real cross-faction aggression — the Mongrel assaults the base on its own. (2) Projectiles: `ProjectileObject` on the weapon + an object with `DumbProjectileBehavior` (bezier params FirstHeight/SecondHeight/PercentIndents, `DetonateCallsKill = Yes`) + `FXListDie`-style death chain works end-to-end; damage lands at detonation exactly once. (3) Terrain scorch texture name is **hardcoded** `EXScorch01.tga` (TerrainTex.cpp): 4×4 UV grid, SCORCH_PER_ROW=3, marks spaced 1.5 cells → cells (0,0),(1,0),(2,0),(0,1), alpha-blended 32-bit TGA; gentex generates ours. (4) Audio: `AudioSettings` needs the full block authored (sample counts/volumes are zero-default traps too); native macos-vulkan AND wasm presets both use MiniAudio (`SAGE_USE_MINIAUDIO=ON`) — backend logs device on boot; SoundEffects `AudioEvent` fields from INIAudioEventInfo.cpp; voice hooks are ThingTemplate fields (VoiceSelect/Move/Attack/Created), weapon `FireSound`, FXList `Sound` nugget. eSpeak NG + DSP chain (tools/gensfx.py) produces the processed-radio barks. (5) Engine console banner still prints the EA title — rebrand-sweep item.
- **D016 dozer loop VERIFIED end-to-end (2026-08-23 overnight).** CC queues WP_Surveyor (KindOf DOZER + DozerAIUpdate) → `MSG_DOZER_CONSTRUCT` (args: int templateID, location, real angle; selected group = the dozer) → BuildAssistant places UNDER_CONSTRUCTION structure, dozer drives over and builds it out (BuildTime honored), → WP_WarFactory (ProductionUpdate + DefaultProductionExitUpdate) queues tanks. `WP_AUTOTEST=base` drives the whole loop headlessly (BASE_LOOP_OK). CommandButton `Command = DOZER_CONSTRUCT` + `Object =` is the UI path; the placement-cursor flow still needs an interactive test. Gotchas: none new — the engine path is solid stock. Lesson from the first run: an auto-acquiring enemy within `vision*GuardOuter` (210) of the RALLY POINT chased in and razed the defenseless base mid-test (CC died, lose-script named-object path fired, shroud re-crept). Map guard units belong outside acquire range of spawn/rally traffic.
- **Icon-data saga (2026-08-23, overnight) — four fixes from one crash.** (1) Original crash: dozer idle-repaired the damaged CC → drawHealing → `newInstance(Anim2D)(NULL template)` because the icon Anim2D templates didn't exist in our dataset. All 10 Drawable icon-creation sites now null-guard (engine). (2) The icon data chain is Animation block → MappedImage → TGA: `Data/INI/MappedImages/HandCreated/*.ini` (loaded by ImageCollection), `Animation2D.ini` — and **the block keyword is `Animation`, not `Animation2D`** (the filename lies; INI.cpp block table). Wrong keyword = "Unknown block" = bare-int throw = the release build's silent "Uncaught Exception during initialization" with no file/line — the engine now prints `FATAL INI: [LINE/FILE] Unknown block` before throwing (permanent diagnosis win). (3) `MappedImage` `Status = NONE` is invalid (bit-string list has no NONE token). (4) Anim2D/MappedImage parsers previously returned WITHOUT consuming their block when their collection didn't exist yet, desyncing the reader — both now consume to End (engine). Also: **macOS deploy recipe** — `cp` over an existing executable can leave a stale kernel signature cache → instant SIGKILL (exit 137) on a binary `codesign -v` calls valid; always `rm` first, `cp`, then `codesign -s - -f`. And SIP strips `DYLD_LIBRARY_PATH` through /usr/bin/perl / lldb wrappers — never test-launch the engine through them (dlopen of the DXVK dylibs fails and it dies with a misleading D3D error).
- **WASM AUDIO ROOT CAUSE (2026-08-23): threadless miniaudio starves every sound.** Upstream's wasm fix sets `MA_RESOURCE_MANAGER_FLAG_NO_THREADING`/`jobThreadCount=0` (no pthreads) with a comment claiming synchronous loads need no job pumping — wrong: miniaudio requires the HOST to drain `ma_resource_manager_process_next_job()` in that mode; even non-async `ma_sound_init_from_file` sounds enqueue init/decode jobs. Result: `ma_sound_start` returns success, the sound lists as playing, but its **cursor stays pinned at 0** (data source never delivers a frame) → engine mixes pure silence while the device pulls fine. Fix: bounded job-pump (16/frame) in `MiniAudioManager::update` under `__EMSCRIPTEN__`. Diagnosis trail that finally cracked it: `[AUDIOREQ]`/`[AUDIO]`/`[AUDIOHB]`/`[AUDIOSND]` traces (IG_TRACE-gated, now permanent) comparing native (cursor advances) vs wasm (cursor 0). Debug-lab gotchas recorded along the way: hidden Browser-pane tabs stall WebAudio rendering AND RAF (drive the engine via `MainLoop.func()` from JS to pump frames manually; audio measurements are only valid while the tab is actually visible); the debug page must set `window.IG_TRACE` (EM_ASM gates) alongside `ENV.IG_TRACE` (getenv gates); `read_console_messages` persists across page reloads (a "triple boot" that was three page loads); SIP strips `DYLD_*` under perl/lldb wrappers (native engine dies with a fake D3D error); macOS kills cp-overwritten binaries via stale signature cache (`rm` + `cp` + `codesign -s - -f`).
- **WASM AUDIO — REAL root cause (2026-08-23, supersedes the job-queue theory above): zero-channel audio buffers.** The wasm (non-FFmpeg) decode path called `ma_decode_memory` with `ma_decoder_config_init(ma_format_s16, 0, 0)` ("native" channels/rate) and then built the `ma_audio_buffer` from `decCfg.channels` — but ma_decode_memory does NOT write the actual format back into the config, so every buffer was created with **0 channels**: reads deliver zero-byte frames, the sound's cursor pins at 0 forever, `ma_sound_start` still returns success, and the engine mixes pure silence. Native never hits it (FFmpeg branch computes channels from decoded frames). Fix: request a concrete output format (`s16, 2, 44100` — miniaudio converts) + `[AUDIODEC]` trace proving real frame counts (22050-mono sources double their frame count ✓). The earlier resource-manager job-pump theory was wrong (buffer-path sounds enqueue no jobs) and is reverted; explicit `ma_sound_group_start` calls added as belt. Lab note: the trace gate statics latch at FIRST call — openDevice-time traces need the flag set BEFORE main() (the page's preRun), not from a later console eval.
- **WASM AUDIO SOLVED (2026-08-23, user-confirmed audible): TWO stacked bugs.** (1) Zero-channel decode buffers (`ma_decode_memory` doesn't write the format back into the config — request a concrete s16/2ch/44100 output; see entry above). (2) **Sound groups never enter the node graph on this emscripten build** — sounds attached to any `ma_sound_group` have their node processor never invoked (graph endpoint reads fine, `justRead=2048 res=0`, but the group's endpoint attachment is dead; explicit `ma_sound_group_start` doesn't help). Fix: attach sounds directly to the engine endpoint under `__EMSCRIPTEN__` (per-sound volumes unaffected; group master faders unused by us). Both are upstream-PR candidates for GeneralsXWeb. Confirmed by ear after a probe build showed `sound-node process call #1` fire only with the bypass. Side-issues logged for later: GameSounds occasionally rejects select-barks (voice limit tuning); `W3DFS_MISS Locater01.w3d` spam (engine hunts a locater model in userdata); wasm logic clock crawls under RAF while rendering runs (FramePacer vs throttled clocks — investigate with the perf pass).
- **Roster batch 2 + canon rename (2026-08-23 afternoon).** Internal names now match creative.md: Fabricator/Power Array/Vehicle Plant (was Surveyor/Power Station/Vehicle Works — the engine base-mode autotest strings renamed too, native rebuild required). New: WP_Outrider (Tank2 conversion, Meridian recon, scout locomotor 45), WPJ_Vulture (Tank1, Jackal gun truck), WP_Zenith (Tank4, Meridian artillery: WP_ZenithGun range 280/min 60, arcing WP_ZenithShell FirstHeight 26, ProjectileDetonationFX WP_FX_ZenithHit with scorch+boom), WPJ_Rigger (Jackal dozer), WPJ_ChopShop (factory, no power — canon: Jackal has no power structure), WP_Bulwark + WPJ_Watchpost (static defenses: STRUCTURE + CAN_ATTACK + AIUpdate w/ auto-acquire + WP_DefenseGun — structure-with-weapon works with a plain AIUpdateInterface). CP now has a command set (Mongrel+Rigger) + production exit. Waves: second team teamWavePack (2 Vultures + 1 Mongrel) on its own 240s/120s timer alongside the 90s/75s Mongrel pairs. Preview row extended to the full cast.
- **Skirmish AI investigation (2026-08-23, findings — implementation deferred).** Player wiring: `playerIsHuman=false` → PLAYER_COMPUTER → gets an `AIPlayer`; with map `skirmish` flag or AIData `ForceSkirmishAI = Yes` it becomes `AISkirmishPlayer`. The skirmish brain is DATA: `Data\Scripts\SkirmishScripts.scb` (compiled WorldBuilder script library — the AI's build/attack logic is literally scripts) + per-side base BUILD LISTS in the map's SidesList (we currently write an empty build list) + auto-reinforcing teams (`teamAutoReinforce`, production conditions). Path of least resistance when we take this on: (1) author a base build list for PlayerB in genmap (ordered structure placements) and let plain AIPlayer construct it (needs a dozer — verify AIPlayer queues one), (2) auto-reinforce attack teams replacing the timer waves, (3) only then consider generating a minimal .scb (its format = the same Script chunks we already emit, wrapped in a script-library container — parseable from ScriptList::ParseScriptsDataChunk paths). Placement cursor models Locater01/02 (engine-hardcoded names, were missing → W3DFS_MISS spam + invisible placement cursor) now generated by genw3d. Voice barks: `Control = INTERRUPT` lets select/move preempt a playing voice (GameSounds rejected same-object overlaps).
- **Combat got real (2026-08-23): smoke-test tactics had to grow up.** With return fire + auto-acquire live, the old autotest lost twice: (1) parking a lone tank inside enemy guard range = arrives pre-shredded (move orders don't retaliate; attack orders do — order attacks, don't stroll); (2) a 1v1 mirror duel is mutual destruction — the loser's last shells are already in flight and land posthumously (projectiles outlive their shooter; 200hp lead evaporated exactly). Full-mode autotest now fields TWO tanks (queue 2, group-select, survivor promotion on first death). Balance data: pre-retune waves (90s grace/75s) razed an undefended CC by 2:15; retuned to 150s/90s (packs 300s/150s) a fleet-play run wins cleanly before wave pressure lands. Radar layout, command-bar icon verification, and the CP-production idle (enemy CP has a build set but no AI to press buttons) are the open UI/AI threads.
- **HUD click-eating root cause (2026-08-23): overlapping context windows.** CommandWindow and ProductionQueueWindow shared one rect; while producing, the invisible queue buttons sat on top — command clicks landed on queue slot 1 = CANCEL production, next click re-queued (money cycles back: "my clicks do nothing"). Fix: disjoint rects (commands 2 rows at y468; queue strip y566). Also: the 3D viewport overdraws the bar down to ~UI y465 — UI elements above that line get hidden behind the scene where terrain renders. Tooltips: ControlBarPopupDescription.wnd (genwnd now emits it — StaticTextName/Cost/Description children) + TOOLTIP:Cost string = hover popups with name+cost. Cameo panel goes portrait→dark during production (engine clears it in the build context — the user's "black box"; now populated whenever a portrait exists). All three residuals from this entry RESOLVED next day — see the 2026-08-23 HUD/dozer entry below.

- **Dozer-stall root cause + construction/queue UX round (2026-08-23 afternoon).** (1) **Stalled translucent sites**: ANY player command to a dozer cancels its current build task (`DozerAIUpdate::aiDoCommand` default branch), and placing a second building cancels the pending first (`newTask` allows one BUILD). The abandoned site sits at ~1HP translucent forever — reads as "destroyed building ghost"/"construction not happening" (both user reports were this artifact). Engine fix (88a74364): idle dozers auto-resume own abandoned UNDER_CONSTRUCTION sites within 600wu in the bored check, ahead of auto-repair (`findAbandonedConstructionSite` + `aiResumeConstruction`); place-several-build-in-turn falls out for free. Click-site-with-dozer manual resume also works (engine path existed all along; clicking own site with a dozer selected issues resume, NOT selection). Verified live in-pane: PA abandoned for Bulwark → Bulwark built → PA auto-resumed 22%→done; move-order interrupt → dozer returned by itself and finished (54%→100%). (2) **"Constructing... -2147483648%"**: `UnicodeString::format` given `%d` with a Real (vswprintf reads garbage) — CONTROLBAR:UnderConstructionDesc now uses `%.0f%%` (both call sites: Drawable world text + control-bar context share the string). Format-spec traps: our str strings must match the arg types at the format call site; CONTROLBAR:Requirements takes a wide string → `%ls` on POSIX vswprintf. (3) **Queue tiles blank**: production-queue buttons draw the **ThingTemplate `ButtonImage` field** (NOT SelectPortrait, NOT the CommandButton's ButtonImage) — all 14 templates now set it; AND populateBuildQueue hardcodes `Command_CancelUnitCreate`/`Command_CancelUpgradeCreate` CommandButtons (INI names) — defined now (Command = CANCEL_UNIT_BUILD / CANCEL_UPGRADE). Queue-tile click = cancel+refund (verified, incl. its own hover tooltip via DescriptLabel). (4) **Tooltip 3rd row** = CommandButton `DescriptLabel` string; all 13 buttons have flavor descriptions now. Popup layout contract (ControlBarPopupDescription.cpp): parent min height 102, desc row measured word-wrapped, desc+parent grow by overflow and parent slides UP by it → the popup is bottom-anchored; ours docks above the command grid at (210,352)-(500,454). Cost row auto-hides when cost 0. (5) **Cameo black box**: engine intentionally nulls the portrait while the queue shows; CameoWindow bg/border alpha 0 = clean empty (W3D window draw skips alpha-0 fills — confirmed live). (6) Missing strings surfaced by play: DOZER:ConstructionComplete, OBJECT:Prop (added). Fabricator borrows Vector voice events (per-unit VO still roadmap).
- **Live-driving the wasm pane (2026-08-23)**: `computer` clicks/screenshots can time out while the pane is hidden — synthetic DOM events straight to the canvas work (PointerEvent+MouseEvent pairs, coords = screenshot px × viewport/800); SDL modifier state comes from key events, but even synthetic ControlLeft keydown did not produce force-attack (clicks select instead) — untested path, use game messages if force-fire matters. Logic crawls under pane throttling: drive `MainLoop.func()` in tight loops (~60 renders/s at 760px viewport ≈ 10-15 logic fps; shrink the viewport to speed up). Native background runs MUST go through `./run.sh` (VK_ICD_FILENAMES/VK_DRIVER_FILES env) — direct binary launch dies "Vulkan Portability library doesn't implement VK_KHR_surface"; native stdout is block-buffered through the run.sh pipe, so a quiet log until exit is normal without IG_TRACE. **Native test launch recipe (forgetting the map arg costs you 30 min of 'why is it idle'):** `WP_PRESENT_SKIP=1 WP_AUTOTEST=base ./run.sh -win -noshellmap -file Maps/WPTest.map` — without `-file` the engine boots to the shell MainMenu and idles at ~3% CPU forever (WP_AUTOTEST only acts once isInGame(); it does not load a map itself). Same args the web page passes via Module.arguments. An idle menu process samples as FramePacer/nanosleep — check CPU time, then `sample` the pid before assuming a hang.

- **The "destroyed building ghost" mystery CLOSED (2026-08-23 evening) + production blackout.** Root cause of the no-label translucent husk: `drawablePostDraw` (W3DView.cpp) skips ALL icon UI (health bar, "Constructing... N%") for objects whose shroud status is worse than PARTIAL_CLEAR, and **construction sites never look** (Object.cpp `handlePartitionCellMaintenance` early-outs on UNDER_CONSTRUCTION) — so an abandoned site outside friendly vision = translucent model (own objects always render) with NO label. Same artifact as the labeled site, different fog state; the user read the unlabeled variant as "a destroyed building left a ghost". Direct experiments (live pane): completed CC destroyed in view by a wave -> removed clean + scorch; fresh 1HP site shot in view by the guard -> removed clean. Fix: own-player objects are never fog-culled from icon UI (leaks no intel) — every own site now shows its label everywhere. Related fixes same commit (engine 9e02b106): ControlBar keeps the portrait while the queue is visible (the "picture becomes black when construction happens" report — the engine nulled the portrait during production by design, our layout has separate queue real estate), and `winTextLabelToText` was a raw-copy stub (@todo TEMPORARY from 2002!) so WND `TEXT = "WP:Cancel"` rendered the key name — now resolves via TheGameText when the label contains ':'. Cancel-construction button restyled: bright red, real "Cancel" text, moved off the cameo overlap ((100,486)-(190,514)).
- **Generals.str is read as ANSI** — UTF-8 em-dashes/typographic chars mojibake in-game ("â€""). Keep .str pure ASCII (sweep in place; a genstr lint would be nice). Missing strings surfaced by play (all added): GUI:CantBuildNotFlatEnough/NoClearPath/ObjectsInTheWay/RestrictedTerrain/Shroud/There/TooCloseToSupplies, WP:Cancel. Known missing, cosmetic, not yet added: GUI:Closed, GUI:SelectNextIdleWorker(+Description), GUI:PlayerInfoListLabel*, SCIENCE:GeneralName.
- **Map-gen follow-up**: the dune noise leaves almost no legal build spots outside the two flattened aprons — "not flat enough" rejections everywhere on open sand. Buildable-area pass needed (more flattened pads or gentler noise).
- **No defeat condition**: player CC destroyed -> match just continues (radar dies with the CC since it carries RadarUpgrade). Defeat/victory UI = roadmap.

- **The husk, actually solved this time (2026-08-23 late): it was the armed PLACEMENT PREVIEW.** The user's translucent no-label building with a live Fabricator beside it was the placement-cursor drawable of a still-armed (or leaked) build placement. Root cause chain: `Mouse::isClick` gates right-click cancel (CommandXlat + SelectionXlat) on `DragTolerance`/`DragToleranceMS`/`DragTolerance3D` — **all zero-default; Mouse.ini must author them** (ours was an empty stub). With zeros, `timeMsDelta > 0` makes every human click a "drag" → right-click cancel of placement NEVER fired → the preview parks at its last terrain point when the mouse moves over the UI (screenToTerrain fails → position stops updating) and reads as an immortal ghost building; the next terrain click places it by surprise (accidental-buy trap, reproduced live). Fixes: Mouse.ini authored (10px / 300ms / 20 3D); engine safety nets in `InGameUI::handleBuildPlacements` — placement auto-cancels when the source dozer dies, and any preview drawable that outlives the mode is destroyed next frame. **Fog-memory ghosts are innocent and work**: `WP_AUTOTEST=ghost` (spawn neutral structure beyond base vision, tank scouts → retreats → target killed fogged → re-scout) with IG_TRACE `[GHOST]` breadcrumbs proves snapShot-on-fog / freeSnapShot-on-relook / orphan removal, natively green. Autotest gotchas burned in: autotest prints must use fprintf(stderr, ...) — stdout is block-buffered through run.sh pipes; shroud/ghost transitions only evaluate for DRAWN objects, so aim `TheTacticalView->lookAt` at the test target or nothing ever snapshots; the guard Mongrel camps (720,560) — route test traffic west.
- **Match results wired**: map scripts WP_Win/WP_Lose (already present) now use DEFEAT (was invisible LOCALDEFEAT); `Menus/Victorious.wnd`/`Defeat.wnd`/`LocalDefeat.wnd` authored by genwnd (big gold VICTORY / red DEFEAT banner; ScriptActions loads these, input freezes, end-game timer runs). Untested live — observe next organic match end.
- **Map buildability**: FLAT_R 190→320, dune amplitude 2.2→1.2 in genmap — the old values left almost no legal build ground off the aprons.

- **Fog was invisible — the husk saga's true root (2026-08-23 night).** User repro (place a site next to the enemy guard; guard kills it before the dozer arrives; "ghost remains"): the site died in a genuinely FOGGED cell — but our fog didn't LOOK like fog, because the object shroud pass (`ShroudTextureShader`: `D3DTSS_TCI_CAMERASPACEPOSITION` texgen + texture transform, multiplicative sprite blend) silently no-ops through DXVK-d3d8/d8web. Consequences before the fix: never-seen enemies rendered plainly (the guard was visible from boot!), fogged objects rendered fully lit, so legitimate fog-memory ghosts looked like live buildings and "died in my sight" was actually "died on camera, outside unit vision". Fixes (engine 0ada123e): scene skips rendering OBJECTSHROUD_SHROUDED objects and renders OBJECTSHROUD_FOGGED ones with `m_foggedLightEnv` (pre-existing, fogAlpha/clearAlpha = 127/255 half-light; ghosts already used it); under-construction sites are excluded from the fog-memory handoff in `PartitionManager::unRegisterObject` (scaffold ghosts = confusion, not intel). Verified live: repro now leaves only a scorch. Note: projected-texture shroud on OBJECTS remains unimplemented on our stack (terrain shroud has its own working path); the light-env approximation covers gameplay. Follow-up candidates: fog darkening also for drawable-less render objects at PARTIAL_CLEAR, per-cell soft edges.

- **Husk, final layer (2026-08-23): LIVE fogged own sites froze into snapshots.** The death-time ghost exclusion missed the alive case: an abandoned own site in an unlooked cell got snapShot'd (translucent model frozen, real drawable pulled from scene) — snapshots have no drawable, so no icon UI ever, immune to the drawablePostDraw fix. Rule that ends the whole class (engine 0b786aae): `PartitionData::getShroudedStatus` short-circuits to CLEAR when the querying player OWNS the object — own objects render live+labeled everywhere, never snapshot, never fog-hide; enemy/neutral fog memory unchanged. Verified live: site abandoned 330wu from anything renders with its "Constructing... 0%" label.
- **Build stamps**: engine boot banner prints `build __DATE__ __TIME__` (native stderr / wasm console); genwebstage injects a corner badge + console line into the staged page ("staged <ts> / engine <wasm mtime>"). First question for any user report: what does the badge say.
- **Open observation — initial map reveal**: the play area starts BRIGHT (cells acting looked) with black only at the far corners; fog forms only where a unit looks and then leaves (scout trails darken). Never-seen enemies on that initial bright ground are visible (the guard shows from boot even after the shroud render fix, which correctly hides SHROUDED objects — these cells apparently aren't SHROUDED). genmap writes no shroud/reveal chunk — engine default seems to pre-look most of the map. Proper ZH start = black shroud + base reveal; needs a map-chunk or engine-init pass. Roadmap item.

- **THE husk, actual root cause (2026-08-23, engine bd885e9f): `MaxLineBuildObjects` zero-default → zero-length placement-icon array → EVERY placement leaked its preview.** `GlobalData::m_maxLineBuildObjects = 0` by default; our GameData never authored it; `InGameUI::init` allocated `m_placeIcon = new Drawable*[0]`; arming placement stored the preview OUT OF BOUNDS and `destroyPlacementIcons` (loop bound 0) freed nothing. The leaked translucent preview sits exactly under the real building (same click position) — invisible until the building DIES, at which point "the destroyed building left a ghost". This is why the artifact tracked destruction perfectly while every death-path/ghost/fog theory tested clean. Fixes: GameData authors `MaxLineBuildObjects = 25`; engine clamps the array to >= 1 slot. Verified via the new scripted UI-flow repro: exactly two `[DEATH] destroyDrawable WP_VehiclePlant` lines (preview at commit + site at death) and a clean scene census.
- **Husk-hunt toolkit (kept, all IG_TRACE/env-gated)**: `[DEATH]` breadcrumbs in `GameLogic::sendObjectDestroyed` + `GameClient::destroyDrawable`; `RTS3DScene::wpDumpRenderObjects()` + `WP_SCENE_DUMP=<frame>` one-shot scene census (name/class/pos/drawinfo per render object — orphan hunting); `WP_AUTOTEST=husk` drives the REAL UI placement (InGameUI::placeBuildAvailable + raw MSG_RAW_MOUSE_* through PlaceEventTranslator — the only way to cover preview lifecycle in a script); the web page forwards `?autotest=`/`?scenedump=` to ENV under `?debug=1`, so any autotest mode runs scripted in the browser.
- **Debug-methodology landmines burned in**: read_console_messages ROTATES — grep the page's DOM log pane (`wpLog` helper pattern) for ground truth, never conclude "trace absent" from the console; the wasm autotests self-advance on background timers (a run can finish during your waits); synthetic pane clicks work only in some sessions — prefer scripted `WP_AUTOTEST` modes over manual pane driving for anything evidentiary; `cd engine` when already inside engine/ silently aborts compound commands (use absolute paths in && chains).

- **Per-unit VO shipped (2026-08-23 night)**: every voiced unit now has its own eSpeak voice/pitch/cadence/grit + its own lines (Vector/Outrider/Zenith/Fabricator + Mongrel/Vulture/Rigger), wired per-template in Object.ini and SoundEffects.ini. The Fabricator no longer says "Vector ready". **Placeholder-sharing is now a build failure**: `tools/lint_voices.py` (run as a genwebstage gate) fails the stage if two templates share a Voice* event, an event is missing from SoundEffects.ini, or a WAV is missing on disk. Pattern to reuse: when placeholder cross-wiring is unavoidable mid-bring-up, it must trip a lint or a logged TODO — silent reuse reads as shipped content.

- **Content push (2026-08-23 night, items 1-7 + tech debt)**: EVA announcer (Eva.ini + WP_EVA_* events + f3 command-net voice; fires live - "Structure lost" on building deaths, base-under-attack etc.); DEFEAT screen verified in-browser (red banner + end-game flow; WP_AUTOTEST=defeat mode); construction scaffolds (genw3d WPSCAF01/02 + AWAITING/PARTIALLY/ACTIVELY condition states on all 7 structures - frame-dump verified); faction select (PlayerTemplate FactionWP/WPJ with Sides, genmap --faction=jackal -> WPTestJ mirrored map, menu picker persisted in localStorage; args must be rebuilt at DEPLOY click, not page load); scripted skirmish opponent (enemy base at 1200,1200: factory/power/2 towers/3 defenders + third assault wave tier at 8min/4min repeat; victory = raze the CP); infantry class (WP_Warden/WPJ_Scrapper: genw3d minifigs, pixel-art portraits in the icon sheet, own VO, SMALL_ARMS damage type with armor scalars - infantry 100%, vehicles 30%, structures 20%; trained at CC/CP; verified live incl. voice + tooltip); tech debt: wasm throttled-tab logic catch-up (up to 10 updates/RAF tick in igrotekaFrameTick), WP_VOLUME env + persisted menu slider -> ma_engine_set_volume, faction+volume persistence via localStorage.
- **New zero-default landmine + the permanent fix**: `ZAxisBehavior = NO_Z_MOTION` in a Locomotor killed boot SILENTLY (exit 0, log ends mid-init) - unknown INI FIELDS died via compiled-out DEBUG_CRASH. INI.cpp now prints `FATAL: Unknown field` to stderr before the crash macro, same as unknown blocks. Never debug a silent init death again: the field name is in the log.
- **Known follow-ups**: WP_AUTOTEST=full (2-tank fleet vs now-defended enemy base) will fail as-is - re-tune the fleet or accept base mode as the gate; initial-reveal anomaly still blocks the true black-shroud start (WP_FogStart script runs, MAP_SHROUD_ALL no-ops - engine-level); WPTest money 10000 vs WPTestJ 5000 discrepancy (playerStartMoney handling); balance pass proper once played.

- **In-engine shell shipped (2026-08-23 night, WPShell.cpp) — five WND/engine landmines burned in.** The shell (MainMenu/WPSkirmish/OptionsMenu/WPScore + QuitMenu/MessageBoxes) is our layouts + our callbacks (lexicon: WPMainMenu*/WPSkirmish*/WPOptions*/WPScore*/WPShellShutdown); stock EA menu code stays dormant. Landmines, each cost a forensics loop: (1) **Layout-block callbacks are UNQUOTED** (`LAYOUTINIT = WPMainMenuInit;`) — parseInit/parseUpdate/parseShutdown tokenize on whitespace only, so a quoted name silently resolves to null (window-level SYSTEMCALLBACK= stays quoted). (2) **A pushed layout without LAYOUTSHUTDOWN wedges the shell forever**: push/pop wait for the outgoing layout's shutdown to call TheShell->shutdownComplete(); stock menus do it at transition end; WPShellShutdown (hide + shutdownComplete) is mandatory on every WP screen. (3) **Intro::doPostIntro runs EVERY frame once the intro state machine hits Done** — the m_breakTheMovie render freeze must be armed one-shot (engine fix) or any shell screen's presence re-freezes the scene; symptom was a menu that rendered exactly one frame, then a menu-started match rendering black forever. (4) **TerrainLogic->loadMap wants the LONG map path** (`Maps\WPTest\WPTest.map`); the short `Maps/WPTest.map` form only works via -file because CommandLine.cpp's file-static ConvertShortMapPathToLongMapPath expands it — menu-start symptom: extentW=0, logic runs, zero render objects, empty radar. (5) **W3DGameWinDefaultDraw draws color fills only when WIN_STATUS_IMAGE is ABSENT** — IMAGE+NoImage = draws nothing; that's why USER panels/backdrops were invisible while gadget children painted. Shell panels author ENABLED+NOFOCUS.
- **Menu-start = same path as -file minus the quit**: wpStartMap sets m_pendingFile + MSG_NEW_GAME(GAME_SINGLE_PLAYER) + InitRandom(0); with m_initialFile empty, clearGameData returns to the shell instead of desktop — quit-to-menu came free. Score screen: ScriptActions doVictory/doDefeat/doLocalDefeat call WPRecordMatchResult (ScoreKeeper snapshot; player data is gone by shell time), WPMainMenuUpdate pushes Menus/WPScore.wnd when a result is pending. Verified E2E in wasm: menu→deploy→match→ESC pause (Return/Restart/Options+slider/Abandon+confirm)→menu, and autotest=defeat→DEFEAT→score screen (stats+duration)→Continue→menu.
- **Engine debt (masked, open)**: after clearGameData the dead match world keeps rendering behind the shell (terrain render object survives; stock cover = shell map). Opaque MenuBackdrop child hides it. Also open: per-frame `W3DFS_MISS '.w3d'` empty-model hunt (shell+game; suspect cursor model resolution), options-layout draws over the pause menu without a dim layer (cosmetic).
- **Wasm pane-driving addendum**: SDL3 mouse coords arrive correctly CSS-scaled (permanent IG_TRACE `[MOUSE]`/`[WINHIT]` forensics); synthetic DOM clicks need pointermove BEFORE pointerdown (SDL tracks position from motion) and must go to ONE target (canvas+document double-delivers). Keyboard: dispatch to canvas only, or every key fires twice (the ESC toggle wobble).
- **Boot instrumentation (page)**: performance.mark wpBoot:deploy/engineReady/dataReady/running + [BOOT] log line + window.wpBootReport; local total ~8.7s (engine 0.1 data 0.1 init 8.5) vs the 20s bar. Page boots menu-mode by default (no -file); ?map= forces direct-load; ?autotest= no longer implies -file (autotests act once in-game, so menu-started runs work too).
- **Restart Battle (stock restartMissionMenu) is broken with menu-started matches**: the reload itself succeeds ([WPSHELL] probes show prepareNewGame + startNewGame with extentW=1600, logic clock runs) but the screen stays black with no ControlBar — some client/display state doesn't survive the clearGameData→restart cycle without the stock shell map (same family as the black-scene saga; SCENESKIP trace silent, so not the breakTheMovie freeze). Needs its own forensics round; Abandon → redeploy is the working loop meanwhile.

- **One-menu product flow (2026-08-24).** The page is the landing menu; DEPLOY boots straight into the picked faction's map via the new `WP_BOOT_MAP` env (same start path as -file but WITHOUT m_initialFile, so match end returns to the in-engine shell); the in-engine shell is the between-matches hub (score → redeploy → stand down), restyled to the page's palette (gold primary / ghost / danger buttons, backdrops, dim underlays). `?map=` keeps the -file quit-after-match path for autotests; `?shell=1` boots to the in-engine menu. Two boot-time guards: GameClient's intro-done `showShell()` call must not raise the menu over the already-running match, and Intro::doPostIntro stays one-shot.
- **THE second-match black screen (and Restart Battle): dangling mid-transition window (engine 45x null-guard fix).** Opening the quit menu starts the QuitFull WINFADE; quitting destroys the menu windows while the transition is mid-play. ~GameWindow correctly unlinks (TransitionWindow::unlinkGameWindow nulls the style's m_win) — but the styles' update()/skip()/draw() mid-frame cases deref m_win UNGUARDED (only their START/END cases check). Native: SIGSEGV at null+8 inside FadeTransition::update → the whole clearGameData teardown dies (exit 0, log ends mid-[DEATH]). Wasm: address 0..8 is readable/writable, so it silently writes low memory every frame and the session limps on — the next match loads fine (extentW ok, checkpoints ok, ShowControlBar ok) but renders black with no HUD. One fix (early `if (!m_win)` in every style entry point) cured quit-to-menu teardown, redeploy, and Restart Battle on both targets.
- **Empty `TEXT = "";` in a WND is a native crash**: parseText strtoks the value and strlen(NULL)s the missing token; wasm survives only because address 0 is readable. genwnd omits the field for runtime-filled labels.
- **GameLogic::quit(FALSE) with no quit menu up just OPENS the menu** (canOpenQuitMenu guard) — the player's Abandon works because exitQuitMenu calls quit while the menu is visible. Headless harnesses must call quit twice.
- **Harness additions**: `WP_AUTOTEST=cycle` (match 1 → double-quit → shell → scripted redeploy → match 2 with [WP_AUTO] CYCLE2 display-state diagnostics) is the regression gate for the between-matches flow; `WP_SCENE_DUMP=+N` re-arms per match (dumps whenever the logic frame crosses N after wrapping); startNewGame carries IG_TRACE checkpoints (loadMap extent, controlbar-branch, loadScreenRender-cleared). Lab lesson: a javascript_tool timeout that kills MainLoop.func() mid-map-load corrupts the wasm session (null-function traps later) — drive loads with short pump bursts and treat post-timeout state as poisoned.
- **Final shell architecture (2026-08-24, user direction): the page IS the shell.** Web mode (WP_BOOT_MAP set) quits the engine at EVERY match exit (clearGameData checks the env; Restart Battle survives via the stock setQuitting(FALSE) that follows it) — the in-engine menu/deployment/score screens never appear in the web product (they remain the native dev harness; `?shell=1` restores them in web for debugging). Match stats reach the page via `Module.onMatchResult` (EM_ASM in WPRecordMatchResult at VICTORY/DEFEAT time — player data is gone by teardown); the page renders the result card (VICTORY/DEFEAT/STOOD DOWN + stats + REDEPLOY=reload). In-match ESC pause menu (Return/Restart/Options/Abandon) is unchanged.
- **Final shell architecture rev 2 (2026-08-24, user direction): the ENGINE shell is the only menu; the page is a pure loader.** The page auto-boots on load (no gesture needed for fetch/init; miniaudio's WebAudio glue resumes the suspended context on the first in-menu click) and fades out at the first engine frame; the styled in-engine shell (two-tone spaced-caps wordmark via two left-justified labels with hand-tuned boxes — no right-justify in STATICTEXTDATA — plus frame borders, tracked-caps gold primary) handles everything else. The web-shell quit-at-exit experiment is reverted (matches return to the in-engine shell; this also un-broke WP_AUTOTEST=cycle, which needs the shell back). In-engine volume slider persists via EM_ASM localStorage ('wpVolume', the same key WP_VOLUME boots from). Module.onMatchResult export kept (unused by the page now; useful for analytics later).
- **Overlay control strip (2026-08-24)**: runtime controls live in an HTML hover pill over the canvas (mute/volume/fullscreen; generals.wasm.ltd-inspired, our design) — the JS bridge is `extern "C" EMSCRIPTEN_KEEPALIVE wpSetMasterVolume(int)` calling TheAudio->setVolume, callable as `Module._wpSetMasterVolume`; persistence via localStorage 'wpVolume' (the WP_VOLUME boot seed). The in-engine Options screen is retired (was only the volume slider). Page chrome lesson: the letterboxed #frame's 1px CSS border + page background read as a stray vertical line beside the canvas in fullscreen/wide windows — the stage is now seamless black and the game frames itself (in-engine frame_border windows, which scale with the canvas).
- **Match-exit flicker fixed with a menu curtain (2026-08-24).** Between the Abandon confirm (or post-banner exitGame) and the shell's first paint there are frames where the pause dim is destroyed, the dying world renders raw, and the engine resets — reads as flicker. Fix: `g_wpMenuCurtain` (Core global) — raised in exitQuitMenu (confirm click) and at clearGameData entry, dropped in WPMainMenuInit and at startNewGame's tail (covers Restart and menu→match); while set, W3DDisplay::draw paints a full-screen black fill right before End_Render (over scene AND UI). Verified frame-by-frame in wasm: one frame after Yes the menu is fully painted, zero intermediate world frames; Restart still renders (curtain drops at load end). Extended to the match-END path: with a result pending, WPMainMenuInit does NOT drop the curtain (the score screen is about to push over the menu — dropping there flashes the menu for 1-3 frames); WPScoreInit drops it instead, so the player sees banner -> black -> stats. WPMainMenuUpdate carries a ~2s failsafe (clears the pending result + curtain) so a missing/wedged score layout can't hold a black screen.
- **Order-buttons + rally round (2026-08-24, Phase 2).** Attack-move/guard/stop CommandButtons (glyph sheet from tools/gencmdicons.py, WP_CombatUnitCommandSet on all 7 combat units) verified live in wasm. Three engine landmines burned in: (1) **Rally-set silently requires a Locomotor literally named `BasicHumanLocomotor`** — doSetRallyPoint (GameLogicDispatch) path-probes with it before accepting the point; missing = every attempt fails as "no path". Ours is data/Data/INI/Locomotor.ini's probe-only entry. (2) **W3DWaypointBuffer hardcodes its node model + line texture** (was "SCMNode"/"EXLaser.tga", now WPNODE01/wp_rallyline.tga) and deref'd the model UNGUARDED — with the model missing, the first rally line draw after a successful set null-crashed (wasm "null function"). All six deref sites now null-guard (line still draws without pucks). The crash only became reachable once (1) was fixed — layered-landmine pattern. (3) **The rally gate is dual**: KINDOF_AUTO_RALLYPOINT on the structure AND a ThingTemplate literally named `RallyPointMarker` (ControlBar::showRallyPoint hard lookup) for the persistent flag drawable (ours: WPRALLY01 model). Strings: GUI:RallyPointSet/RallyPointNoPath. Verified in wasm: "Rally point set: Command Center." + flag drawable at the point, no crash.
- **WP_AUTOTEST=full is a real 4-tank fleet now** (wp_fleet[4]; stage 2 waits for all four, bail at f=1500; survivor promotion + reselect loops scan the array). Verdict run: fleet ready f=480, four guard vehicles killed by f=1463, enemy CP razed, clean exit — the all-green regression gate stands (base/cycle/win/defeat/full).
- **Power meter lives (2026-08-24).** The bar was a static rect because the real meter is `W3DPowerDraw` (DRAWCALLBACK on ControlBar.wnd:PowerWindow) and it silently returns without FOUR hardcoded mapped images: PowerPointG/PowerPointY/PowerPointR (repeating tick cells, color = margin: green healthy / yellow within PowerBarYellowRange / red deficit) + PowerBarSlider (consumption needle). Ours live on the wp_cmdglyphs.tga sheet (cells at x=192+; the sheet is 24-bit, so tick separators are painted in trough color, not alpha). Semantics are LOG-scale: bar length = log_base(production) x (width/intervals) with GlobalData PowerBarBase=7/Intervals=3/YellowRange=5 defaults; needle at log(consumption). Two rakes stepped on: (1) a decorative trough/border SIBLING overlapping the meter window wins the input hit-test (draw order shows the meter, input picks the sibling) and eats the hover tooltip - the fix is ONE window whose draw callback paints its own trough+border (W3DPowerDraw now does, WarPowers block); (2) a DRAW CALLBACK replaces the default bg/border paint entirely - a callback window's WND colors never render.
- **Throttled-pane forensics rake (cost a full loop today): a hidden Browser pane freezes rAF, so screenshots composite a STALE framebuffer** - the HUD "vanished" for three straight boots because ShowControlBar landed after the pane stopped presenting; the frame on screen predated it. The wasm session is fine (sim frames advance, input works). Before diagnosing missing UI in the pane: front the pane, deliver one input event, wait a beat, THEN screenshot. Related: [MOUSE]/[WINHIT] traces log button events only - no trace lines from a synthetic hover is normal, not evidence input is dead.
- **Phase 3 infantry round (2026-08-24): procedural W3D skeletal animation shipped.** tools/genrig.py emits HIERARCHY (pivot quat+translation base pose) / ANIMATION (delta channels: Q rotations + XYZ translations, VectorLen 4/1, 12-byte channel head + floats) / bone-bound HLODs against a SHARED skeleton (WPINF1). Engine contracts burned in: anim channels are DELTAS composed onto the base pose (HTreeClass::Anim_Update translate-then-postmul); anim registered name is "HIER.ANIM" and **load-on-demand derives the FILENAME from the part after the dot** (Get_HAnim("WPINF1.WPINF1WLK") loads wpinf1wlk.w3d) - one file per anim, hierarchy in its own wpinf1.w3d (Get_HTree auto-loads "<hier>.w3d"); HLOD sub-objects bind meshes to pivots by BoneIndex with mesh verts in BONE-LOCAL coords; W3D_NAME_LEN=16 everywhere (hier/anim names <=15 chars). INI side: `Animation = WPINF1.WPINF1WLK 5.4` (distance syncs walk rate), **IdleAnimation MUST pair with AnimationMode ONCE** - LOOP throws INI_INVALID_DATA whose DEBUG_CRASH is compiled out in release = SILENT boot death (engine now prints "FATAL INI: error parsing block..." on stderr for every block-parse throw).
- **AudioEvent unknown fields SEGFAULT** (strcmp on a bad table walk in initFromINIMulti), they don't hit the unknown-field print: the valid range fields are MinRange/MaxRange (no "Range"). Crash signature: EXC_BAD_ACCESS in _platform_strcmp under INI::parseAudioEventDefinition.
- **The lab pane throttles LOGIC, not just paint.** A hidden/backgrounded Browser-pane tab runs rAF at ~0 -> game-logic frames crawl (the in-game clock top-right is GAME time - read it before diagnosing). Today's phantom: "production stalled" was 90 wall-seconds = ~2 game-seconds. Drive ALL wasm verification through MainLoop.func() pump bursts (<=1.4s per javascript_tool call) and treat wall-clock waits as ~zero game time. Related boot-order quirk: startNewGame pre-selects the CC before the ControlBar populates - the bar can lag until an input/context poke; clicks during its 500ms slide-in fall through to the WORLD (and with a building selected, a ground click = SET RALLY - the accidental-rally spam pattern).
- **Phase 3 aircraft/maps round (2026-08-24 evening).** VTOL hover class: Locomotor Surfaces=AIR + Lift + PreferredHeight + ZAxisBehavior=SURFACE_RELATIVE_HEIGHT + Appearance=HOVER just works with plain AIUpdate (no JetAIUpdate/parking needed for pad-exit hover craft). The autotest 'canMake' gates print is a CanMakeType ENUM - 0 is OK (misread it as a bool for a whole session); isPossibleToMakeUnit requires the unit in the PRODUCER'S COMMAND SET (anti-cheat) so the CC lab can't spawn pad units. **-file map paths: CommandLine's converter expands EVERY '-file Maps/X.map' to 'Maps\\X\\X.map' - passing the long form yields 'Maps\\X\\X\\X.map', which loads as an EMPTY WORLD (extentW=0): sides never parse, the local player stays the neutral observer, and the observer control bar (windows we don't ship) null-crashed in populateObserverList (now guarded with a stderr warning + [WPSIDE] side-selection trace).** Always pass the SHORT form to -file; only wpStartMap wants long paths. Shell buttons: GadgetButtonSetText is a GGM_SET_LABEL send - our buttons' PassSelectedButtonsToParentSystem drops it; use winSetText directly.
- **genmap layouts**: --layout={flats,ridge,scrap} - a layout = geometry dict (play size, base anchors, optional ridge wall with gap fractions); enemy cluster + waypoints derive from anchors with offsets rotated toward the player. The ridge is heightmap-only (no cliff impassability map painted; steep slope blocks ground pathing in practice - watch pathfinding at the passes in play).
- **HUD click fall-through killed (2026-08-24, user-reported).** The window manager treats a click as UNUSED when the whole hit chain returns MSG_IGNORED - so a click on a DISABLED build button (insufficient funds / full queue), or on any gap between HUD widgets, fell through to the game translators and became a rally-point/move order BEHIND the opaque bar. Mechanics: findWindowUnderMouse -> winPointInChild skips disabled children (playing the engine-hardcoded "GUIClickDisabled" AudioEvent - which we never defined, so it was SILENT) and returns the parent panel, whose default USER input ignores everything. Fixes: (1) `WPHudSwallowInput` on ControlBar.wnd:ControlBarParent consumes button events, EXCEPT an UP whose DOWN happened out in the world (band-select released over the bar still reaches the selection translator); (2) CANMAKE_NO_MONEY restored to COMMAND_CANT_AFFORD (a prior edit had collapsed it to COMMAND_RESTRICTED) - unaffordable buttons keep the COLORED icon (WIN_STATUS_ALWAYS_COLOR) vs prereq-gray; (3) GUIClick/GUIClickDisabled AudioEvents defined (wp_ui_click / wp_ui_deny). Verified in wasm: $400 vs a $600 button -> deny click sound, no charge, no rally; ~20 HUD-area clicks produced zero world leaks. Open cosmetic question: GadgetPushButtonInput's GUIClick on enabled presses never appears in the audio log (deny plays fine through the same addAudioEvent) - park it.
- **Aircraft were never flying (user-caught): the Gravity default is per-FRAME units.** GameData's `Gravity` field parses with parseAccelerationReal (world-units/sec^2 -> per-frame^2), but the CODE DEFAULT when the field is absent is `-1.0` raw - i.e. -1.0 per-frame^2, ~9x stock ZH gravity (-100/sec^2 = -0.111/frame^2). Locomotor lift math (`maxNetLift = maxGrossLift + gravity`, clamped at 0) meant NO Lift value could win: Lift=160/sec^2 = 0.178/frame^2 vs -1.0 -> net 0. The hover craft pathed as AIR units but rode at z=0 the whole time - the "flight" I verified earlier was ground-driving with a SHADOW_DECAL directly underneath (a decal paints under grounded units too; craft-vs-shadow SEPARATION is the real flight tell). Fix: `Gravity = -100` in GameData.ini (stock ZH; also normalizes death-toss physics). Verified: Kestrel exits the pad, climbs to PreferredHeight, hovers at the rally IDLE (the reported case), and holds altitude in motion.
- **Aircraft movement fixed (user-caught round two): airborne objects need TWO locomotor opt-ins.** With the gravity fix in, the VTOLs climbed and then PARKED IN THE SKY ignoring every move order (or, pre-fix, drove on the ground - which is why they seemed to move at all). Root cause chain, proven with the [WPAIR] IG_TRACE (pos/goal/want/fwd2D/vel/dir/turn per 30 frames in locoUpdate_moveTowardsPosition): the AI passed full desiredSpeed and a valid goal, but `Locomotor::locoUpdate_moveTowardsPosition` line ~1077 SKIPS THE ENTIRE 2D MOVE DISPATCH (all turn + thrust) for airborne objects unless **`AllowAirborneMotiveForce = Yes`** - the gate exists so jumping ground vehicles can't steer mid-air, and flying locomotors must opt in. Sibling gate: **`Apply2DFrictionWhenAirborne = Yes`** or Extra2DFriction never applies in flight and momentum turns into giant skating orbits. Full working hover recipe (both WP/WPJ hover locomotors): Surfaces=AIR, Lift, PreferredHeight(+Damping 1.0), SpeedLimitZ, ZAxisBehavior=SURFACE_RELATIVE_HEIGHT, Appearance=HOVER, AllowAirborneMotiveForce=Yes, Extra2DFriction=2.0, Apply2DFrictionWhenAirborne=Yes. Verified over 5+ orders: climb, turn-to-goal from any facing, exact-max-speed cruise, approach decel, settle at hover, attack-move flight - no orbits, no fly-aways. [WPAIR] stays in (IG_TRACE-gated).
- **Phase 3 close-out (2026-08-24 evening): the roster bars.** Scout/heavy infantry on the shared rig (new bodies only - the WPINF1 skeleton + anims carried over free), Shrike/Gnat second airframes, AA towers (WP_FlakGun is AntiAirborne* ONLY - no AntiGround - so AA can't be ground-abused), tech buildings as Prerequisites gates (verified live: deny before, allowed after the Directorate stood), Jackal Dynamo power, pillboxes. **Landmine burned (cost a full forensics loop): genmap's team COUNT was a hardcoded literal - adding a 7th team dict under-declared the list and the sides reader consumed the trailing team as garbage, yielding a map that LOADS (extent/sides fine) but spawns ZERO OBJECTS: no CCs, black shroud everywhere, camera staring at nothing, harness silently idle.** The count is now derived from the collected dicts. Diagnostic that cracked it: lldb attach -> TheGameLogic->m_objList == nullptr. Also: I misread the '50(50)' corner readout as a frozen logic frame TWICE - it is the FPS counter; get the real frame from TheGameLogic->m_frame.
- **Placement-reject feedback was silent**: the engine's stock path (PlaceEventTranslator) already plays 'NoCanDoSound' + dozer 'VoiceNoBuild' + displayCantBuildMessage(lbc) - but the AudioEvent and the GUI:CantBuild* strings were never defined in our data. Both now exist (deny click + 7 reason strings). Same hardcoded-name family as GUIClick/GUIClickDisabled/RallyPointMarker/BasicHumanLocomotor.
- **Playtest findings (self-run, Basin + ScrapJ)**: tech chain, chaingun combat, defeat flow on a new map, scout speed and its heroic death all good. Open quirks: band-select prioritizes combat units so grabbing the dozer amid troops is fiddly and we ship NO idle-worker button (stock ZH has one - UX gap for later); the 2-minute raider wave severely punishes greedy openings (balance note, possibly by design); the basin rim rejects placement correctly but the ghost gives no red tint at a distance.
- **Finishing-touches batch (2026-08-24 late): music, biome, idle button, artillery, VO depth — and two big forensic finds.** Music: 6 CC-BY tracks (Kevin MacLeod, 96kbps/4:30-capped, ~15MB) as `MusicTrack` blocks; there is NO stock rotation — `GameEngine::update` now polls `TheAudio->isCurrentlyPlaying(handle)` and starts the next track (150-frame first delay, 240 between). Verified natively via the lavc `[mp3 @ ...]` decode chatter (which only music produces); the two decoder warnings are cosmetic. Ash biome: `write_tga_ash()` luma-desaturates the ground sheet; `Terrain WPGroundAsh` + genmap `ground=` key; WPScrap/WPRange regenerated — renders correctly. Idle-worker button: `ControlBar.wnd:ButtonIdleWorker` (PUSHBUTTON, WPGlyIdleWorker glyph, PassSelectedButtonsToParentSystem) + `CommandButton NonCommand_IdleWorker` (**Command = NONE is a valid parse** — cleanest no-op command; the ControlBarSystem name-key branch handles the click before command processing, and ControlBar.cpp:1223 wires the tooltip). genwnd's `window()` now takes `image=` (drawdata IMAGE field — that's how WND buttons get icons). **Engine fix**: a dozer that registered idle during map load was orphaned forever — `InGameUI::reset` clears the idle list but `DozerAIUpdate::m_isMarkedAsIdle` stays TRUE, so it never re-registered; the register branch now re-asserts every idle tick (addIdleWorker dedups). Verified: button lights, click selects + centers, doubles as find-my-dozer. Artillery: WP_Longbow/WPJ_Lobber (MERART01/JAKART01 in genw3d, `MinimumAttackRange` dead zone + `ScatterRadius`, slot 9 on both builder sets — **9-slot command sets populate fine**). Prereq gating + tooltip (engine auto-appends "Requires: Directorate") verified live; **live FIRING still untested** (both defeats cut the tech chain short). VO: 6 newest voice sets deepened to 3-deep sel/mov/atk; `artyfire()` boom → WP_SND_ArtyFire.
- **September 5 Retry repairs:** corrected actual mip-filter ownership (the next source was used after releasing its caller reference), retained inner texture storage for surviving surface views, checked surface-copy bounds/pitches and fixed trailing-ampersand/nullable-output errors in sentence layout. The final release passes two consecutive eight-minute native defeat/score/Retry cycles without surface tracing; a separate ASan build passes a shorter lifecycle run. See [verification.md](verification.md) for exact candidates, negative controls and fixture limits. This supersedes the earlier logging-only investigation below.

- **Restart-battle black-screen: crash CAUGHT with a full stack (2026-08-24).** Second deploy after a defeat→score→continue cycle died with a wasm `null function` at: `GameWindowManager::processDestroyList → GadgetStaticTextSystem(GWM_DESTROY) → freeDisplayString → ~W3DDisplayString → ~Render2DSentenceClass → Reset → ~SurfaceClass → D3DSurface->Release()` — an invalid virtual dispatch while releasing a surface. The original over-release diagnosis was not proven by this stack; the September 5 investigation supersedes it. Manual `MainLoop.func()` pumping surfaces the exception that rAF swallows silently — that silent swallow IS the "black screen". It is **intermittent** (identical defeat→redeploy and restart-battle and abandon→redeploy runs all passed afterwards — heap-layout dependent). Mitigation shipped: `RefCounted8::release()` in the d8web fork now guards over-release (logs `[WPSURF] OVER-RELEASE` and returns instead of exploding) — this only checks a live inner wrapper; it cannot protect dispatch through a stale outer bridge wrapper and is not a confirmed fix. Engine-side refcounts in Render2DSentence audited clean; at that point the source remained open (unproven suspects: font-cache surface churn — the money readout recreates a 64x64 font surface EVERY FRAME, a perf smell worth fixing on its own — or a texture-level wrapper pair).
- **Dozer task-wedge near the CC (REPRODUCED 2×, open).** Ordering structure B right as/after structure A completes, with B's site adjacent to the CC, wedges the dozer: it parks near the CC (second repro: clipped INTO the CC's north face), `isIdle()` stays false (so no idle-button rescue, no bored-check auto-resume), the site sits at 1HP, money stays spent. Open-ground single orders work flawlessly; auto-resume works when the dozer is genuinely idle (verified: it drove back to an abandoned site after finishing an interposed Rampart). Workarounds that worked: click-the-site resume (first repro), or just train another Fabricator. Deep-dive pending: DozerAIUpdate task acceptance during the build-complete transition + CC-adjacent approach solving.
- **Score screen stats are all zero (open, player-visible).** Real matches show `Units built 0 / lost 0 / destroyed 0, Structures built 0, Credits earned 0` after building units+structures and spending money. Only Duration is live. The WPScore layout renders; the ScoreKeeper→score-screen plumbing was never wired for our path.
- **Browser lab technique updates**: (1) `window.MainLoop` (emscripten module-scope leak) is the pump handle — `MainLoop.func()` bursts ≤1.4s; needed because the app-pane browser reports `document.hidden=true` when backgrounded and rAF (hence ALL game logic) freezes — screenshots still work, so a "stalled" game that advances only during JS excursions means you forgot to pump. (2) The harness's `key` action does NOT reach SDL3 — dispatch a synthetic `KeyboardEvent` at the canvas (`isTrusted:false` is fine for SDL's listeners); that's how ESC opens the pause menu. (3) miniaudio exposes `window.miniaudio.devices[0].webaudio.state === 'running'` — quick audio-pipeline liveness probe. (4) `-file` REQUIRES the short form `Maps/WPTest.map` (parseFile always runs ConvertShortMapPathToLongMapPath; a long path doubles and the match silently never starts — the game just sits at the shell playing music). (5) Native runs MUST go through `run.sh` (it composes VK_ICD_FILENAMES/VK_DRIVER_FILES/DYLD_LIBRARY_PATH; a bare `./GeneralsXZH` dies with "Vulkan Portability library doesn't implement VK_KHR_surface"). (6) macOS `timeout` does not exist — background the run instead.
- **Post-batch polish round (2026-08-24 late-late): idle-button placement, score stats, Longbow live-fire.** (1) The idle-worker button had been placed INSIDE CameoWindow's rect — with a selection the glyph stamped the portrait's top-left corner (user caught it). Now at (10,567)-(46,597), the corner slot BELOW the cameo; when adding HUD chrome, check the WND rects it overlaps — windows z-fight in declaration order and both draw. (2) **Score stats were all zero because EVERY ScoreKeeper counter is gated on KindOf SCORE** (buildings also honor SCORE_CREATE/SCORE_DESTROY; units need SCORE plus INFANTRY/VEHICLE) — stock templates all carry it, ours never did. All 27 selectable WP/WPJ templates now append SCORE; verified end-to-end in a real browser match (score screen "built 3 / destroyed 4 / structures 3"). "Credits earned" was never broken — AutoDepositUpdate calls addMoneyEarned; a match with no Exchange/Racket legitimately shows 0. (3) **WP_Longbow live firing verified in play**: forward-placed battery auto-acquired the first raider wave (spread craters = ScatterRadius visible) and killed an ordered target on force-attack; min-range dead zone means raiders that reach the base sit UNDER the arc — artillery cannot defend itself or the CC at close range (by design; note for wave balance). Also confirmed again: the dozer wedge does NOT occur when build orders are issued only after the previous build fully completes and the dozer idles — the trigger is specifically ordering during the completion transition. (4) Browser-lab: the pane canvas is pillarboxed — UI x0 lands at pane x≈105 (scale ≈0.7375); convert UI rects before clicking or you click the black bars. Module.onMatchResult (the web-shell stats hand-off) is a handy headless probe: install a JS hook and read the exact struct the score screen renders.
- **Dozer wedge ROOT-CAUSED and FIXED (2026-08-25, the sweep).** The trace (`WP_DOZER_TRACE=1`, `?doztrace=1` in the browser) showed the truth: `newTask` precomputes the build-approach "dock point" from the dozer's position AT ORDER TIME — often across the map, and (for fresh construction) BEFORE the site exists — so it can land unreachably close to or inside the building's footprint. `DozerActionPickActionPosState` then trusted that stored point forever: the move ends short, `DozerActionMoveToActionPosState` fails on outer-idle, PickActionPos re-issues the SAME stale point, the pathfinder completes instantly, loop — dozer parked, site at 0%, money spent, `isIdle()` false so no idle-button rescue and no bored-check auto-resume. The map regen changed base geometry and started tripping it (it was never about order timing — `WP_AUTOTEST=wedge`, the new deterministic gate, wedged on the FIRST order pre-fix). Fix: PickActionPos recomputes the approach with `findGoodBuildOrRepairPosition` from CURRENT positions every time it runs; the dock-action phase already self-positions and tolerates imperfect approach. Wedge gate now green end-to-end (inject 2nd construct at 91% of the 1st → both build → BASE_LOOP_OK). Two forensic landmines burned on the way: (1) `StateMachine::getCurrentStateName()` is STUBBED to empty in release — a `prim=''` trace reads as "null current state" and is meaningless; use `getCurrentStateID()`; (2) a defensive null-state self-heal now lives in `DozerAIUpdate::update` anyway (logs before resetting). Browser-lab addendum: unit-over-structure pick priority means a dozer parked on the CC soaks every click meant for the building — reselect after moving it away, and remember world clicks with a unit selected are ORDERS (three "CP clicks" in a row were Rigger moves; the persistent bar that read as "CP selected" was its damaged-CC health bar).
- **Ambient beds shipped (data-only):** `Control = loop` + `LoopCount = 0` = permanent loop (theAudioControlNames: LOOP/RANDOM/ALL/POSTDELAY/INTERRUPT). Wind bed rides the command centers' `SoundAmbient` (Limit 1 dedups the two CCs, MaxRange 6000 ≈ global); PowerHum on Power Array/Dynamo, Factory loop on Vehicle Plant/Chop Shop. Seamless loops via exact-period synthesis (hum/factory) or tail→head crossfade (wind) in gensfx.
- **Sweep wrap (2026-08-25):** WPJ_Lobber built and engaged raider waves in a real Jackal match (craters across its arc both waves; weapon block is a value-clone of the live-fire-verified Longbow). Victory banner witnessed via WP_FRAME_DUMP on the win gate (-file mode quits after the banner; the shell score path is shared with the twice-verified defeat flow). Score screen again exact in play: units 5 / structures 4 / destroyed 4. User confirmed music audible in browser. New harness knobs: WP_AUTOTEST=wedge (deterministic dozer-wedge gate), WP_DOZER_TRACE / ?doztrace=1.
- **Phase 4 v1 landed (2026-08-26): the enemy is a REAL AIPlayer — base building, economy, trained attack teams.** All data except three trace/hardening engine touches. The machine: `playerIsHuman=false` → PLAYER_COMPUTER → plain `AIPlayer` (skirmish flag would give AISkirmishPlayer); its update loop runs base building (build list + dozer, auto-QUEUES a dozer from any build-list factory via `findDozer→queueDozer`), team production (`selectTeamToBuild`), and rebuilds. What the data must provide, learned the hard way: (1) **the AIPlayer constructor turns unit production OFF** (campaign convention — map scripts must enable it; `AISkirmishPlayer`'s ctor re-enables, the plain one doesn't). Without `PLAYER_ENABLE_UNIT_CONSTRUCTION` in the side's scripts the AI builds its whole base but never trains a single team — the ONLY unit that slips through is the dozer, because `queueDozer` force-toggles the flag. Diagnosed with the new `WP_AI_TRACE` (per-team gate dump: cond/inst/pri + per-factory canMake split). (2) **Team production conditions are scripts referenced by name** (`teamProductionCondition` in the team dict); no script = never build. Ours: `CONDITION_TRUE` (raiders) or `TIMER_EXPIRED(counter)` armed once at match start = permanent tier unlocks (Raider 120s / Pack 240s / Assault 480s / Air 600s; an expired countdown STAYS expired). (3) **`findFactory` searches the BUILD LIST only** — pre-placed production structures get auto-registered by `AIPlayer::newMap`, and `isPossibleToMakeUnit` needs the unit in the factory's command set (the enemy base gained a Roost/LaunchPad so air teams are possible + an Exchange/Racket so production has income). (4) **`Team::tryToRecruit` treats the player's DEFAULT team as always-poachable** and steals from any team with LOWER productionPriority within MaxRecruitRadius — the AI drafted the pre-placed mid-map guard + both base defenders into its first raider team and killed an idle player's CC at 0:45. Fix: pre-placed garrison lives on its own `teamBaseGuards` (priority 100 > every attack team, `teamIsAIRecruitable` false). (5) Build-list wire format (SidesList v3 per side): buildingName, templateName, x,y,z(→0), angle, byte initiallyBuilt, int numRebuilds, script, int health, bytes whiner/unsellable/repairable. Not-initially-built entries = visible in-match expansion (the AI dozer-built a 2nd power, a 2nd income, and a forward tower every run; rebuilds=99). (6) `teamHome` = gather waypoint (EnemyRally at the base front); `TEAM_ATTACK_NAMED(<This Team>, PlayerCC)` on-create for raiders/pack/air, `TEAM_HUNT` for the assault tier.
- **Phase 4 forensics tooling (all env-gated, permanent):** `WP_AI_TRACE` — [WPAI] computer-player economy/army every 10s + per-team production gates + factory split; [WPDESTROY] any NAMED object entering destroyObject prints identity + backtrace (`WPPrintBacktrace`, execinfo — stubbed under emscripten); [WPNAMED] NAMED_DESTROYED evaluations + named-cache nulls. The 45-second "phantom defeat" hunt burned three wrong theories (production-flag default, script byte order, named-cache pointer aliasing) before the destroyObject backtrace showed a REAL weapon kill — the recruit blitz above. Lesson repeated: when a scripted end-condition fires "impossibly", trace the OBJECT's death first, not the condition's plumbing. `evaluateNamedUnitDestroyed` also gained a 3-frame debounce (hardening kept: the cache nulls by pointer match; one anomalous frame must not end a match).
- **Full-gate semantics vs the live AI:** the 4-tank blind rush no longer razes the enemy — the AI's defended, producing base grinds it down (tanks at 25-300hp bogged 150wu short). The gate still exits green (fields, fights, CC safe, clean exit); victory-path coverage stays with WP_AUTOTEST=win. Browser playtest: defense-first opening (Rampart at the approach + 2 Vectors + Exchange) beat waves 1 and 2 of trained raiders arriving down the NE lane on schedule.
- **Phase 4 v2a: difficulty selector (2026-08-26).** Deployment screen gains an OPPOSITION row (same cycler pattern as the battlefield picker): SKIRMISH / STANDARD / BRUTAL, persisted in localStorage 'wpDiff', default STANDARD. Plumbing costs almost nothing because it already existed end-to-end: MSG_NEW_GAME arg 1 → GameLogic::prepareNewGame → ScriptEngine::setGlobalDifficulty → the AIPlayer ctor snapshots it → **executeScript() skips any script whose easy/normal/hard flag byte excludes the current difficulty**. All differentiation is therefore data: per-difficulty one-shot TIMER-ARM scripts (same counter, different values, difficulty-flagged): Raider 240/120/60s, Pack 480/240/150s, Assault —/480/300s, Air —/600/420s — easy simply never arms assault or air, so those TIMER_EXPIRED production conditions stay false and the tiers never exist. Verified headlessly with the new `WP_DIFFICULTY=0/1/2` -file override: EASY raiders' condition flipped exactly at 4:00 with assault locked; STANDARD at 2:00; BRUTAL at 1:00 (idle player dead at 1:52). Restart Battle re-reads the live global difficulty — the choice survives restarts for free. `?aitrace=1` now forwards WP_AI_TRACE to the wasm build. Post-game oddity (pre-existing, cosmetic): the ENEMY CC registers as weapon-destroyed a few seconds after a defeat ends the match — during the banner the sim keeps running; noted, not chased.
- **Phase 4 v2b: reactive behaviors (2026-08-26) — Phase 4 CLOSED.** Three new AI teams, all data + one engine stub implemented: (1) **Defense patrol** — priority-45 trained garrison replacement, `CONDITION_TRUE` production condition, on-create `TEAM_GUARD` at the base front; verified: fielded within the first minute of every match and re-fielded when killed (inst 1/1 maintained). (2) **Punish** — priority-40 retaliation squad whose production condition is `PLAYER_DESTROYED_N_BUILDINGS_PLAYER(PlayerA, 1, PlayerB)`. **That stock condition was an unimplemented stub** (`@todo CLH implement me! return FALSE`); implemented via a new `ScoreKeeper::getTotalBuildingsDestroyedOfPlayer(idx)` accessor over the existing per-victim `m_totalBuildingsDestroyed[]` buckets (fed by kill-credit in `Object::scoreTheKill`, gated on the victim's side being playable + our SCORE KindOf). Verified deterministically with the new `WP_AUTOTEST=strike` harness (4 tanks kill the AI's isolated forward tower): bldKilled bucket ticks, condition flips, retaliation team trains. **Scoring nuance: an UNDER-CONSTRUCTION building kill does not count** (stock gate) — a browser kill of the tower mid-rebuild flipped nothing and burned an hour as a phantom bug. (3) **Eco-raid** — priority-28 fast movers (Outrider/Vulture ×3) unlocked by RaidTimer (normal 7:00 / brutal 4:00, never on easy); on-create applies a named ATTACK_PRIORITY_SET (default 1, player's income 60 / power 40 — set up by a one-shot at match start via `SET_DEFAULT_ATTACK_PRIORITY` + `SET_ATTACK_PRIORITY_THING`, template-name based to stay enum-proof) then `TEAM_HUNT` — the hunt prefers the player's economy. Verified with a temporary 30s arm: team trains, APPLY executes, no crashes. Parameter ordinals used: OBJECT_TYPE=15, ATTACK_PRIORITY_SET=28. **AISkirmishPlayer/.scb path RETIRED** — the data-driven AIPlayer brain (build list + expansion + escalation tiers + difficulty + defense/punish/eco-raid) covers the Phase 4 bar without the skirmish-sides/scb integration cost; revisit only if stock skirmish behaviors (supply lines, multi-base) become requirements.
- **Phase 5 opening pass (2026-08-31): budgets measured, matrix started, publish prep staged.** (1) **Frame budget on M-series: flat 60/s** sustained through a BRUTAL match's raider/pack/assault phases (measured by MainLoop pump-rate with rAF suspended — each `MainLoop.func()` is a full engine frame, so calls/sec = real frame capacity; 53.7/s only during match load-in). Deep-late-game mega-army stress remains an open checklist item; the money-readout font churn stays parked as perf debt with no measured symptom. (2) **Local boot 5.5s cold** (engine 165ms / data 214ms / init 5.2s — init dominates; a real-network CDN run will shift the download legs). (3) **Boot beacons**: the page now fires `GET /wp-boot-ok?total=…&ua=…` on reaching the main loop and `/wp-boot-fail?…` on engine abort — they land in ANY static server's access log, so browsers we can't drive (Safari/Firefox) are verifiable server-side. **Safari: boots clean, total=751ms** (AppleWebKit/605 UA in the log after the full manifest fetch). Firefox is not installed on this machine. (4) **Trademark audit**: user-visible surfaces (page copy, tab title, all of Generals.str, menus) carry zero EA marks; the devtools-visible engine-lineage identifiers (GeneralsXZH.js, Generals.str filename, CNC_GENERALS_* env names, GeneralsX console banner) are deliberate attribution-adjacent internals, documented in docs/publish-checklist.md. (5) **Vercel prep staged, NOT deployed** (deploys stay user-gated): vercel.json serves webstage/ only (.vercelignore excludes everything else), wasm content-type header, 1h asset cache — NOT immutable, because genwebstage paths aren't content-hashed yet (the `?v=` query on fetches is a page-side cache-buster, not a path hash); switch to immutable after hashed paths. docs/publish-checklist.md is the living gate list.
- **Staging landmine (found 2026-08-31 during the polish sweep):** `tools/genwebstage.py` stages gamedata **from the runtime dir** (`~/GeneralsX/GeneralsZH`), not from `data/` — so repo edits to Data/Maps/Art/Window reach the browser only after the repo→runtime sync (`rsync -a data/<d>/ ~/GeneralsX/GeneralsZH/<d>/` for the four data dirs). It had silently not run since Aug 26; the tell was a five-day-old `Generals.str` inside a "fresh" stage. genmap's no-arg default also writes the runtime copy, which masks the staleness for maps. Fixed same day: genwebstage now stages straight from `data/`; the runtime dir is a native-deploy target only.

## Player-experience integration (September 2026)

The browser shell now consumes a versioned `Module.onGameState` snapshot
from `WPShell.cpp` and receives actual `onMatchResult` / mission messages.
Keep the telemetry observational: native map scripts decide objectives and
results. The snapshot uses one contiguous numeric buffer plus string pointers;
large scalar `EM_ASM` calls can compile yet fail when a referenced `$16` is not
present in the generated JavaScript signature. Copy buffer values synchronously
inside the callback and never retain a pointer into its stack frame.

HTML panels acquire and release only their own pause via `wpSetWebPause`.
Native Escape/P pauses must survive closing an HTML panel. Gameplay/rendering
controls are written to `Options.ini` before startup; changing their settings
requires restart. Channel audio and overlay appearance apply immediately.

Checkpoint storage mounts IDBFS at the engine's synthetic browser user directory.
A native save writes `Save/wp-checkpoint.sav`; a sidecar records compatibility
and mission identity, then the page flushes to IndexedDB. Native portable map
paths are lowercased in saves, so the bridge canonicalizes the known dataset
map IDs after load. Restore the command bar position before a briefing can pause
its entrance animation. Serialize save requests across panel reopenings.

Every map-placed object with a Draw module needs a Body module, including
noninteractive scenery. `Drawable::onLevelStart` asks that body for its damage
state while preparing ambient sound. The original missing scenery bodies caused
a WASM null-function exception on map load; the content validator now prevents
this data error.

Native test fixtures must complete normal Create-module lifecycle callbacks.
In particular, `SupplyCenterCreate::onBuildComplete` registers a completed depot
with the resource manager. Merely allocating a depot yields a misleading test
where trucks collect but cannot discover a delivery destination. Identify newly
produced trucks by producer ID; ordinary skirmish maps also have starting
haulers. Map-placed War Powers haulers are explicitly put into their normal
harvesting state on fresh game start, while restored saves retain their orders.

`Appearance = FOUR_WHEELS` changes native movement as well as wheel posing.
It requires an explicit, attainable `MinTurnSpeed`: `LocomotorTemplate` defaults
that field to `BIGNUM`, and `moveTowardsPositionWheels` scales steering by
`actualSpeed / MinTurnSpeed`. Omitting it makes vehicles drive almost straight
past their goals even when `TurnRate` is high. The source is
`GeneralsMD/Code/GameEngine/Source/GameLogic/Object/Locomotor.cpp`; increasing
turn rate or changing supply docking modules does not address the missing
denominator. Our wheel locomotors use 15 world units/second for scouts, 6 for
heavy artillery, 10 for Porters and 12 for Scavengers. `CanMoveBackwards = Yes`
also lets them reverse during close approaches. Keep `W3DTruckDraw` and the
wheel bones; reverting the art is unnecessary.

This failure was reproduced in build `6375`: both native economy diagnostics
showed zero cargo or stock removal, with only passive hub income. The corrected
`6b10fd8f36e1` passed both 60-second runs with actual delivery and maximum cargo
of four crates for the Porter and two for the Scavenger. Their player cash
changes were $2,100 versus $300 passive and $1,240 versus $240 passive,
respectively. Cash and source-stock totals also include starting haulers, so
these are delivery regression checks, not isolated truck-throughput or balance
measurements. `tools/validate_gameplay.py` now rejects missing or unattainable
wheel turn speeds and checks reverse capability; an in-memory reproduction of
the missing-field data was rejected without changing shipped files. New wheel
locomotor changes still require native movement and harvesting checks.

`WP_AUTOTEST=mission` changes explicit prerequisites, target survival or timer
expiry and observes native script counters/results. It never dispatches a
victory action itself. The expected-result latch is separate from `inGame`:
the latter remains true during the end-game banner. Diagnostic sessions must
not persist their forced wins into a player's operation record.

`WP_REVIEW_SCENE=1` creates a bounded faction roster review on the Flats;
`stress` adds a bounded mixed-army encounter. These scenes bypass production
for visual/performance inspection. They are separate from input-driven tests,
normal matches and evidence about balance. See `docs/verification.md` in the
parent workspace for current results and exact limitations.
